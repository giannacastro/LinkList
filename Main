// node in the linked list
class Node {

  // where data value is stored
  int item;
  // for the next node
  Node next;

  // constructor for a new node
  Node(int item) {
    // sets the item value
    this.item = item;
    // initalizes next to null
    this.next = null;
  }
}
